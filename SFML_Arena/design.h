#pragma once
#include <SFML/Graphics.hpp>

static const inline sf::Color backgroundInterfaceColor = sf::Color(170, 170, 170, 200);
static const inline sf::Vector2f buttonRatio = sf::Vector2f{ 300.0f, 100.0f };

static const inline sf::Color normalTextColor = sf::Color(127, 127, 127, 255);
static const inline sf::Color normalButtonColor = sf::Color(200, 200, 200, 200);
static const inline sf::Color darkerButtonColor = sf::Color(200, 200, 200, 200);
static const inline sf::Color disabledButtonColor = sf::Color(120, 120, 120, 200);

static constexpr float padding = 20.0f;